#!/bin/bash

TAG=$1
DOCKERFILE=$2
FILE_TO_INJECT=$3

DOCKERFILE_INJECTED=`mktemp`

cat $DOCKERFILE > $DOCKERFILE_INJECTED
cat $FILE_TO_INJECT >> $DOCKERFILE_INJECTED

cat $DOCKERFILE_INJECTED

docker build -f $DOCKERFILE_INJECTED --tag $TAG .
